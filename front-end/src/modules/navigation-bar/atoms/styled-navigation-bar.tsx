import { AppBar } from '@mui/material';
import styled from '@emotion/styled';

const StyledAppBar = styled(AppBar)`
  position: fixed;
  top: 0;
  width: 100%;
  height: 70px;
`;

export { StyledAppBar }
