import { Toolbar } from '@mui/material';
import styled from '@emotion/styled';


// NOTE: (Alopez) Observation; It only wraps the ProShop Logo and Nav Bar
// Components. 
const StyledToolbar = styled(Toolbar)`
  background: #10950B;
`;

export { StyledToolbar }
